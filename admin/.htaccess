# admin/.htaccess - Extra protection

# Only allow if user is admin (we'll check in PHP too)
# But basic deny direct PHP access except index.php
<Files ~ "\.php$">
    Order deny,allow
    Deny from all
    <Files "index.php">
        Allow from all
    </Files>
</Files>
